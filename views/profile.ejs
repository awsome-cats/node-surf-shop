<% layout('layouts/boilerplate') -%>
<h2><%= currentUser.username %>のプロフィール</h2>
<p>最近の投稿</p>
<% posts.forEach(function(post) {%>

    <div>
        <a href="/posts/<%= post.id %>"><%= post.title %></a>
    </div>

<% });%>
